{
  "name": "muster-code",
  "version": "0.2.0",
  "private": true,
  "description": "Muster Code — the standalone Codex-first coding environment built on Code-OSS around muster.",
  "license": "UNLICENSED",
  "scripts": {
    "build": "pnpm --filter @muster-code/builtin build",
    "assemble": "zsh scripts/assemble.sh",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@10.33.2"
}
