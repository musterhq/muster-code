# Orchestrator continuation notes

User requires major implementation by Luna High subagents, root only orchestration/review/live browser+native QA. No recursive delegation or unnecessary repeated passes. Explicit full IDE replacement scope now includes true agent hierarchy/messages/changes/controls and usable thread management. Graphite default, multiple customizable palettes, realtime/full-file diffs protected.

Agents `/root/runtime`, `/root/frontend`, `/root/browser` are all gpt-5.6-luna high. Runtime owns agent-pane/codex/context/conversation/core adapter. Frontend owns view/polish/settings/themes. Browser agent now owns thread catalog plus extension/manifest wiring, after browser/terminal work. All same filesystem. Latest assignments correct orchestration adapter semantics and UI contract, child control support, and real open/continue thread integration.

Artifacts: kanban.json authoritative, kanban.md, kanban.html (server4180), qa-plan.md, product-contract.md, usage.jsonl, lane notes and parity/workspaces design. No card is Done yet; native gates are incomplete.

QA scratch repo /tmp/muster-qa-20260913 (README marker MAPLE-47; fixtures/notes with spaces.md marker ORBIT-29; full-file.ts 500 lines; copied site fixture). Isolated app profile /tmp/muster-qa-profile-20260913 with Luna High defaults and accessibility on. CLI launch is documented app command. Never infer against user’s real projects.

Staged dist/verified/Muster Code.app was assembled for the earlier integrated hardening batch. It does NOT yet contain the newest Graphite defaults, final inspector persistence/layout fixes, or new orchestration/thread-catalog wave. Rebuild only when current batch is frozen.

Evidence: baseline real native prompt “Reply with READY only. Do not use tools.” returned READY, completed7s on Sol Medium. After isolated config changed, new task visibly had Luna High. Actual follow-up file/context/inference flow still untested. Root integrated pass43 tests + typecheck/bundle; core agent earlier reported737 core tests. Later changes have focused agent tests, not yet one consolidated gate.

Native CUA has had stale-window/noWindowsAvailable/SCK invalid-parameter errors after restart. User manually brought QA window foreground on request; AX then recovered and showed real restored READY transcript and Luna High new task. Next action was interrupted; do not classify control failures as product bugs. Runtime diagnosed current/staged bundle matching65ca559... at prior stage and no source startup fault.

Offline real-renderer fixture /tmp/muster-polish-preview, server4178, browser1 tab1. Stress mode ?scenario=stress:72messages+24reasoning nodes,40events,80deltas,24refs,20ledger rows. Root found activity overflow hiding composer; frontend fixed max height/internal scroll. Root verified vertical bounds normal and short/narrow with1216-char draft. Found nested inspector open states lost on reload and fixture-only horizontal banner overflow; both fixed and root retest passed: both inspectors open after reload, no horizontal overflow, composer within viewport (logical345x563 due browser zoom). Graphite preview verified rgb20,20,20 with#B0B8F8 accent. Temporary viewport currently narrow; reset before final handoff.

Browser server4317 serves QA site directory. Board server4180 serves this docs folder. Root only samples account weekly usage at milestones:7%baseline,8%wave1,9%freeze; native later showed10% but latest official checkpoint9%. Account-wide, not all attributable to this session. No credits redeemed.

Keep pending native QA/Full Access/recovery/browser-to-source/terminal/agent workflow gates honest. CUA per-action security confirmation policy applies to changing Full Access via UI; do not quietly bypass genuine permission requests.
